<?xml version="1.0" encoding="utf-8"?>
<!-- animateLayoutChanges makes the pop-out smooth natively! -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/chatHeadContainer"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="8dp"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:animateLayoutChanges="true"> 

    <!-- 1. The Chat Head Icon -->
    <FrameLayout
        android:id="@+id/chatHeadIcon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/bg_chat_icon"
        android:elevation="8dp">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="14dp"
            android:src="@drawable/ic_ai_shield" />
    </FrameLayout>

    <!-- 2. The Message Bubble -->
    <TextView
        android:id="@+id/notificationText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/bg_chat_bubble"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:textColor="#F9FAFB"
        android:textSize="14sp"
        android:fontFamily="sans-serif-medium"
        android:maxWidth="240dp"
        android:maxLines="3"
        android:ellipsize="end"
        android:visibility="gone"
        android:elevation="6dp"
        android:shadowColor="#000000"
        android:shadowDx="0"
        android:shadowDy="1"
        android:shadowRadius="2"/>

</LinearLayout>