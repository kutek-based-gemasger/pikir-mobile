import 'package:flutter/material.dart';

import '../../../core/dev/placeholder_screen.dart';

/// Rincian cara beban utang dihitung terhadap penghasilan.
///
/// Phase 1 stub. Replace the body with the real screen; the route and the
/// entry in the screen registry already point here.
class RasioUtangScreen extends StatelessWidget {
  const RasioUtangScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(
      title: 'Detail Rasio Utang',
      reference: 'SCREENS.md layar 9 - P1',
      note: 'Rincian cara beban utang dihitung terhadap penghasilan.',
      showBottomNav: false,
      overlay: false,
    );
  }
}
