import 'package:flutter/material.dart';

import '../../../core/dev/placeholder_screen.dart';

/// Ringkasan perlindungan yang sudah aktif.
///
/// Phase 1 stub. Replace the body with the real screen; the route and the
/// entry in the screen registry already point here.
class PenyiapanSelesaiScreen extends StatelessWidget {
  const PenyiapanSelesaiScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(
      title: 'Penyiapan Selesai',
      reference: 'SCREENS.md layar 7 - P2',
      note: 'Ringkasan perlindungan yang sudah aktif.',
      showBottomNav: false,
      overlay: false,
    );
  }
}
